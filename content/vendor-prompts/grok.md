### Output Style

One-line goal. No preamble. State which files change.

Use XML markers for file content:
```xml
<file path="src/utils/foo.ts">
// code here
</file>
```

Verification: 3 bullets max. No meta-commentary. Flag breaking changes inline as `⚠ [reason]`.
