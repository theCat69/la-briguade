import type { Config } from "../types/plugin.js";

/**
 * Register skill paths from content/skills/ into the config.
 * Each skill directory contains a SKILL.md file discoverable by opencode's skill tool.
 */
export function registerSkills(_config: Config, _contentDir: string): void {
  // TODO: implement — register content/skills/ path into config.skills
}
