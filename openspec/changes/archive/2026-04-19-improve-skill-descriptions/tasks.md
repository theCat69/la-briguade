## 1. Finalize proposal and capability scope
- [x] 1.1 Read the existing change scaffold and context snapshot for `improve-skill-descriptions`.
- [x] 1.2 Define capability set for precise skill descriptions, auto-inject description-line behavior, and code-example index alignment.
- [x] 1.3 Confirm proposal language stays aligned with repository terminology and scoped to documentation-driven behavior.

## 2. Produce required spec artifacts
- [x] 2.1 Create capability spec for precise skill-description purpose/scope boundaries.
- [x] 2.2 Create capability spec for optional auto-inject description-line wrapping behavior.
- [x] 2.3 Create capability spec for code-example index synchronization on description-related pattern updates.
- [x] 2.4 Ensure each spec uses OpenSpec requirement/scenario structure with testable outcomes.

## 3. Capture cross-cutting design decisions
- [x] 3.1 Add a concise design artifact describing decisions, trade-offs, and non-goals across skills, auto-inject skills, and code-example indexing.
- [x] 3.2 Verify design decisions map directly to proposal capabilities and spec requirements.

## 4. Mark change apply-ready
- [x] 4.1 Create `tasks.md` in parseable checkbox format.
- [x] 4.2 Mark all checklist items complete to satisfy apply-required `tasks` status.
- [x] 4.3 Confirm only OpenSpec change artifacts were added for this change.
